# 6월 15일 TIL

## 구글 로그인 / 리액트 / TypeScript

일단, client_id 값이 조회되지 않는다는 오류를 마주하였다.

콘솔에 찍어보니, .env 파일에 작성해둔 클라이언트 아이디가 불러와지지 않는 문제가 있다.

지금 프로젝트 구조처럼 frontend 디렉토리 밖에 .env 파일 생성해두고, 활용하는게 맞는 방식인가?

테스트하기 위해서 frontend 디렉토리에 환경변수 파일 생성해서 불러오니 잘 동작한다. 근데 환경변수 파일이 두 개가 공존하는 형태가 괜찮나?

그리고 차후에 배포할 때는 어떻게 해결해야될지도 감이 전혀 오지 않는다..

일단 버튼 자체가 disabled 되어 있었던 이유는 유효한 client 값을 받아오지 못해서였던 것 같다
